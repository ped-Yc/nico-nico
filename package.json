{
  "private": "true",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "cli:dev": "npm run build && npm run docs:dev",
    "cli:build": "npm run build && npm run docs:build",
    "cli:preview": "npm run build && npm run docs:preview"
  },
  "keywords": [
    "nico",
    "vue",
    "framework"
  ],
  "author": "ped-yc",
  "license": "ISC",
  "peerDependencies": {
    "vue": "^3.2.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitepress": "^1.1.4"
  },
  "workspaces": [
    "packages/*",
    "play",
    "docs"
  ],
  "dependencies": {
    "@nico-nico/nico-cli": "workspace:^",
    "nico-nico": "link:",
    "pnpm": "^9.0.6"
  }
}